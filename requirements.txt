gradio
openai-whisper
pyannote.audio>=3.0 # Specify >=3.0 for potential Hugging Face API changes
torch # Note: Specific version might be needed depending on CUDA setup and other libraries
torchaudio
pydub
demucs # For optional vocal separation
yt-dlp # For URL downloading

# Dependencies for Sound Event Detection (CLAP) & Whisper/Pyannote Backends
transformers # For CLAP model
librosa # Handles audio loading/resampling for CLAP and others
# soundfile - Usually installed as a dependency by librosa/torch

# YAML Output
PyYAML 